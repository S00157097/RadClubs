@model ClubManager.Models.ClubModels.Club

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Club</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ClubName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ClubName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CreationDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CreationDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.AdminId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AdminId)
        </dd>
    </dl>
</div>
<p> 
    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
    @if (User.IsInRole("ClubAdmin") || User.IsInRole("Admin"))
    {
        @Html.ActionLink("Edit", "Edit", new { id = Model.ClubId }, new { @class = "btn btn-primary" })
    }
</p>


<div>
    @Html.Action("_ClubEvents", "ClubEvents", new { id = Model.ClubId })
</div>